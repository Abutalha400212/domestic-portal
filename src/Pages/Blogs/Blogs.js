import React from 'react';
import useHooks from '../../Hooks/useHooks';

const Blogs = () => {
    useHooks('Blogs')
    return (
        <div>
            
        </div>
    );
};

export default Blogs;